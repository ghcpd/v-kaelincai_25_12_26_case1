You are a senior software engineer. You have been given a project with a buggy AVL tree implementation that is causing database index failures. Complete the following tasks in a single comprehensive workflow:

## PART 1: ANALYZE AND FIX THE BUG

### Task 1.1: Understand the Bug
Read and analyze the existing project located at C:\BugBash\workSpace1\issue_project:
1. Read KNOWN_ISSUE.md - Complete bug description
2. Read src/avl_tree.py - The buggy implementation (focus on _insert_recursive() method, lines 128-132)
3. Read tests/test_avl_tree.py - Failing unit tests
4. Read README.md - Project documentation
5. Read requirements.txt - Project dependencies
6. Read src/user_database.py - Database implementation using AVL tree
7. Read tests/test_user_database.py - Integration tests
8. Read src/__init__.py and tests/__init__.py - Module initialization files

The bug is in the Left-Right (LR) rotation implementation which causes:
- Tree structure corruption
- Loss of balance property
- Performance degradation from O(log n) to O(n)
- Potential data loss during rotations

### Task 1.2: Create Fixed Version
Create a NEW directory at C:\BugBash\workSpace1\oswe-mini-m23a2s165\issue_project_fixed with the complete project structure:

```
issue_project_fixed/
├── src/
│   ├── __init__.py
│   ├── avl_tree.py          # FIXED VERSION - correct LR rotation order
│   └── user_database.py     # Copy from original (no changes needed)
├── tests/
│   ├── __init__.py
│   ├── test_avl_tree.py     # Copy from original
│   └── test_user_database.py # Copy from original
├── README.md                # Updated to indicate this is the fixed version
├── requirements.txt         # Same as original
└── FIX_SUMMARY.md          # NEW FILE: Document your fix
```

### Task 1.3: Implement the Fix
In src/avl_tree.py:
1. Locate the buggy LR rotation code (around lines 128-132)
2. Fix the rotation order in the LR case - implement the correct sequence
3. Add clear comments explaining the correct implementation
4. Do NOT modify LL, RR, or RL rotation implementations (they are correct)

The buggy code section:
```python
# Left-Right (LR) case - **BUG IS HERE**
if balance > 1 and key > node.left.key:
    # Current wrong implementation
    node = self.right_rotate(node)
    node.left = self.left_rotate(node.left)
    return node
```

The correct fix should:
- Perform the child left_rotate FIRST (straighten the zig-zag)
- Then perform the parent right_rotate SECOND (balance the tree)

Correct implementation:
```python
# Left-Right (LR) case - FIXED
if balance > 1 and key > node.left.key:
    # 1) rotate left on left child
    node.left = self.left_rotate(node.left)
    # 2) rotate right on parent
    return self.right_rotate(node)
```

### Task 1.4: Create Documentation
Create FIX_SUMMARY.md with:
- Problem Identified: What was wrong with the original implementation
- Root Cause: Why the wrong rotation order caused the bug
- Solution Implemented: What changed and why
- Code Changes: Side-by-side comparison (before/after)
- Technical Explanation: The correct LR rotation algorithm and why this order is necessary
- Verification: Test results showing all tests pass

Update README.md to indicate this is the FIXED version and remove the "Known Issues" section.

### Task 1.5: Run Initial Tests
After creating the fixed project, run a quick test to verify the fix:
- Navigate to the fixed project directory
- Run: `python -m pytest tests/ -q`
- Verify all tests pass (expected: 9 passed in total)

## PART 2: COMPREHENSIVE VALIDATION

After initial verification, execute a complete validation workflow:

### Task 2.1: Install Dependencies
Install all project dependencies:
1. Navigate to the fixed project directory: C:\BugBash\workSpace1\oswe-mini-m23a2s165\issue_project_fixed
2. Run: `pip install -r requirements.txt`
3. Capture output and verify no errors

### Task 2.2: Run Automated Tests (pytest - verbose)
Execute pytest test suite with verbose output:
1. Run: `python -m pytest tests/ -v`
2. Capture complete test output
3. Verify all 9 tests pass
4. Expected output format:
   ```
   test_lr_rotation_basic PASSED
   test_lr_rotation_with_search PASSED
   test_lr_rotation_complex_sequence PASSED
   test_lr_rotation_performance PASSED
   test_ll_rotation PASSED
   test_rr_rotation PASSED
   test_rl_rotation PASSED
   test_user_database_lr_scenario PASSED
   test_bulk_user_insertion PASSED
   ====== 9 passed in X.XXs ======
   ```

### Task 2.3: Run Automated Tests (unittest - verbose)
Execute unittest discovery for cross-verification:
1. Run: `python -m unittest discover tests -v`
2. Capture complete test output
3. Verify all 9 tests pass
4. Expected output format: "Ran 9 tests in X.XXXs OK"
5. Confirm consistency with pytest results

### Task 2.4: Runtime Demonstration Test
Execute a runtime validation test to verify the system launches and behaves correctly:

Run a Python one-liner command:
```bash
python -c "from src.user_database import UserDatabase; db=UserDatabase(); db.add_user(1000, {'name':'John'}); db.add_user(500, {'name':'Jane'}); db.add_user(750, {'name':'Bob'}); print('IDs ->', db.get_all_user_ids()); print('Balanced ->', db.is_index_balanced()); print('User 750 ->', db.get_user(750))"
```

Expected output:
```
IDs -> [500, 750, 1000]
Balanced -> True
User 750 -> {'name': 'Bob'}
```

This verifies:
- Database instantiation works
- Insert operations trigger LR rotation (1000, 500, 750 sequence)
- Tree remains balanced after LR rotation
- Inorder traversal returns sorted sequence [500, 750, 1000]
- Search/lookup operations work correctly

### Task 2.5: System Launch Verification
Verify that:
- All modules import successfully without errors
- UserDatabase instantiates correctly
- No runtime exceptions occur during basic operations
- Public API functions as expected

### Task 2.6: Consistency Verification
Ensure consistent behavior:
- Test results are consistent across pytest and unittest frameworks
- Runtime demo confirms expected behavior
- All validation steps complete without errors, exceptions, or tracebacks

## PART 3: COMPREHENSIVE REPORTING

### Task 3.1: Capture All Outputs
Document the following:

**Locations:**
- Fixed project directory: issue_project_fixed
- Main fix: avl_tree.py (LR rotation order)

**Actions Performed:**
1. Installed project test dependencies from requirements.txt
2. Ran the complete test suite with:
   - pytest (`python -m pytest tests/ -v`)
   - unittest discovery (`python -m unittest discover tests -v`)
3. Executed a short runtime demo of UserDatabase
4. Captured console outputs and test logs

**Results (Captured Outputs):**
- Dependency install: status (packages installed or already present)
- pytest output: "Collected 9 tests — all passed" and "9 passed in X.XXs"
- unittest output: "Ran 9 tests — OK"
- Runtime demo output: IDs, Balanced status, User lookup result
- Note: "No errors, exceptions, or tracebacks observed during any step"

### Task 3.2: Create Validation Summary
Provide a comprehensive summary:

**Conclusion — Test Status & Functional Verification:**
- All automated test cases passed (9/9)
- The system launches and runs without errors in the fixed project
- Runtime behavior verified:
  - Insert/search/inorder traversal work correctly
  - AVL index remains balanced in LR scenarios
- The LR rotation fix is confirmed by passing unit & integration tests and by runtime checks

### Task 3.3: Final Status Report
Provide a clear final statement:

```
All automated test cases passed (9/9).
The system launches and runs without errors in the fixed project.
Runtime behavior verified: Insert/search/inorder traversal work correctly, AVL index remains balanced in LR scenarios.
The LR rotation fix is confirmed by passing unit & integration tests and by runtime checks.

Final statement: **All test cases passed and the project behaves correctly under the provided test scenarios.**
```

## SUCCESS CRITERIA

Your work is complete when ALL of the following are true:

1. ✅ New directory issue_project_fixed is created with complete project structure
2. ✅ The LR rotation bug is fixed with correct operation order (child first, parent second)
3. ✅ ALL 9 unit tests pass (0 failures)
4. ✅ ALL integration tests pass
5. ✅ Dependencies installed successfully (pip install -r requirements.txt)
6. ✅ pytest execution completes with all 9 tests passing (verbose mode)
7. ✅ unittest execution completes with all 9 tests passing (verbose mode)
8. ✅ Runtime demo test executes successfully
9. ✅ System launches without errors
10. ✅ Tree remains balanced after LR rotations
11. ✅ Search operations work correctly after rotations
12. ✅ Inorder traversal returns sorted sequence
13. ✅ FIX_SUMMARY.md clearly documents the fix with code comparison
14. ✅ README.md updated to indicate fixed version
15. ✅ All test outputs captured and documented
16. ✅ Final validation summary explicitly states: "All test cases passed and the project behaves correctly under the provided test scenarios"

## REQUIREMENTS AND CONSTRAINTS

### Must Do:
✅ Create the fixed project in NEW directory (do not modify original)
✅ Read ALL source files including __init__.py files
✅ Fix ONLY the LR rotation bug (child rotate first, parent rotate second)
✅ Install dependencies before running tests (pip install -r requirements.txt)
✅ Execute comprehensive validation (pytest -v + unittest -v + runtime demo)
✅ Capture all test outputs, logs, and results
✅ Create complete documentation (FIX_SUMMARY.md)
✅ Update README.md to indicate fixed version
✅ Run tests with both frameworks for cross-verification (pytest and unittest)
✅ Execute runtime demo using one-line Python command
✅ Explicitly state whether all test cases passed
✅ Document any warnings, errors, or exceptions (even non-critical ones)
✅ Verify 9 tests pass in total

### Must NOT Do:
❌ Do NOT modify the original project directory
❌ Do NOT change the test files
❌ Do NOT modify LL, RR, or RL rotation implementations
❌ Do NOT add new dependencies to requirements.txt
❌ Do NOT skip validation steps
❌ Do NOT use multi-line Python commands in terminal (use semicolon-separated one-liners)

### Code Quality:
- Maintain the same code style as the original
- Keep comments clear and concise
- Ensure proper indentation and formatting
- Follow Python best practices
- Add explanatory comments for the fixed LR rotation code

## VALIDATION EXECUTION CHECKLIST

Execute these steps in order:

1. ✅ Read KNOWN_ISSUE.md, src/avl_tree.py, tests/test_avl_tree.py
2. ✅ Read README.md, requirements.txt
3. ✅ Read src/user_database.py, tests/test_user_database.py
4. ✅ Read src/__init__.py and tests/__init__.py
5. ✅ Create issue_project_fixed directory structure (directories first)
6. ✅ Copy and create all files (fix avl_tree.py, copy others)
7. ✅ Create FIX_SUMMARY.md documentation
8. ✅ Update README.md
9. ✅ Run initial test (python -m pytest tests/ -q)
10. ✅ Install dependencies (pip install -r requirements.txt)
11. ✅ Run pytest with verbose output (-v flag)
12. ✅ Run unittest discovery with verbose output (-v flag)
13. ✅ Execute runtime demo (single-line Python command)
14. ✅ Capture all outputs and logs
15. ✅ Create comprehensive validation summary
16. ✅ Provide final status report

## EXPECTED FINAL OUTPUT

Your final response should include:

### Summary
- LR rotation bug fixed ✅
- Fixed the LR (Left-Right) rotation order in avl_tree.py
- Preserved original files and tests (no modifications to original project)
- Added FIX_SUMMARY.md and updated README.md
- Ran the test suite — all tests passed

### What Changed
- File updated: avl_tree.py
  - Fixed LR case in _insert_recursive()
  - BEFORE (wrong): right-rotate parent, then left-rotate child
  - AFTER (correct): left-rotate node.left first, then right-rotate node
  - Added comments explaining the proper double-rotation order

### Why This Fixes the Bug
- LR is a double-rotation. Correct sequence:
  1. left-rotate the left child (straighten zig-zag)
  2. right-rotate the parent (rebalance)
- Reversing the sequence corrupts links and can make nodes unreachable
- The fix restores the AVL invariant and search correctness

### Full Validation Report

**Locations:**
- Fixed project directory: issue_project_fixed
- Main fix: avl_tree.py (LR rotation order)

**Actions Performed:**
1. Installed project test dependencies from requirements.txt
2. Ran the complete test suite with:
   - pytest (python -m pytest tests/ -v)
   - unittest discovery (python -m unittest discover tests -v)
3. Executed a short runtime demo of UserDatabase
4. Captured console outputs and test logs

**Results (Captured Outputs):**
- Dependency install: pip reported required packages already installed (no errors)
- pytest output: Collected 9 tests — all passed, Summary: 9 passed in X.XXs
- unittest output: Ran 9 tests — OK
- Runtime demo output:
  ```
  IDs -> [500, 750, 1000]
  Balanced -> True
  User 750 -> {'name': 'Bob'}
  ```
- No errors, exceptions, or tracebacks observed during any step

**Conclusion — Test Status & Functional Verification:**
- All automated test cases passed (9/9)
- The system launches and runs without errors in the fixed project
- Runtime behavior verified:
  - Insert/search/inorder traversal work correctly
  - AVL index remains balanced in LR scenarios
- The LR rotation fix is confirmed by passing unit & integration tests and by runtime checks

### Final Statement
**All test cases passed and the project behaves correctly under the provided test scenarios.**

---

Complete all tasks autonomously in a single workflow without requiring follow-up instructions. Ensure every validation step is executed and documented. Use verbose mode for test execution and one-line Python commands for runtime demos.
