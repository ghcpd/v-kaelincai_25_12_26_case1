You are a senior software engineer. You have been given a project with a buggy AVL tree implementation that is causing database index failures. Complete the following tasks in a single comprehensive workflow:

## PART 1: ANALYZE AND FIX THE BUG

### Task 1.1: Understand the Bug
Read and analyze the existing project located at C:\BugBash\workSpace2\issue_project:
1. Read KNOWN_ISSUE.md - Complete bug description
2. Read src/avl_tree.py - The buggy implementation (focus on _insert_recursive() method, lines 128-132)
3. Read tests/test_avl_tree.py - Failing unit tests
4. Read src/user_database.py - Database implementation using AVL tree
5. Read src/__init__.py and tests/__init__.py - Module initialization files
6. Read README.md - Project documentation
7. Read requirements.txt - Project dependencies

The bug is in the Left-Right (LR) rotation implementation which causes:
- Tree structure corruption
- Loss of balance property
- Performance degradation from O(log n) to O(n)
- Potential data loss during rotations

### Task 1.2: Create Fixed Version
Create a NEW directory at C:\BugBash\workSpace2\oswe-mini-m23a1s255\issue_project_fixed with the complete project structure:

```
issue_project_fixed/
├── src/
│   ├── __init__.py
│   ├── avl_tree.py          # FIXED VERSION - correct LR rotation order
│   └── user_database.py     # Copy from original (no changes needed)
├── tests/
│   ├── __init__.py
│   ├── test_avl_tree.py     # Copy from original
│   └── test_user_database.py # Copy from original
├── README.md                # Updated to indicate this is the fixed version
├── requirements.txt         # Same as original
└── FIX_SUMMARY.md          # NEW FILE: Document your fix
```

### Task 1.3: Implement the Fix
In src/avl_tree.py:
1. Locate the buggy LR rotation code (around lines 128-132)
2. Fix the rotation order in the LR case - determine and implement the correct sequence
3. Add clear comments explaining the correct implementation
4. Do NOT modify LL, RR, or RL rotation implementations (they are correct)

The buggy code section:
```python
# Left-Right (LR) case - **BUG IS HERE**
if balance > 1 and key > node.left.key:
    # Current wrong implementation
    node = self.right_rotate(node)
    node.left = self.left_rotate(node.left)
    return node
```

The correct fix should:
- Perform the child left_rotate FIRST (straighten the zig-zag)
- Then perform the parent right_rotate SECOND (balance the tree)

### Task 1.4: Create Documentation
Create FIX_SUMMARY.md with:
- Problem Identified: What was wrong with the original implementation
- Root Cause: Why the wrong rotation order caused the bug
- Solution Implemented: What changed and why
- Code Changes: Side-by-side comparison (before/after)
- Technical Explanation: The correct LR rotation algorithm
- Verification: Test results showing all tests pass

Update README.md to indicate this is the FIXED version and remove the "Known Issues" section.

## PART 2: COMPREHENSIVE VALIDATION

After creating the fixed project, execute a complete validation workflow:

### Task 2.1: Environment Setup
1. Configure Python environment for the fixed project at C:\BugBash\workSpace2\oswe-mini-m23a1s255\issue_project_fixed
2. Create or use existing virtual environment
3. Verify Python environment is properly configured

### Task 2.2: Run Automated Tests (pytest)
Execute pytest test suite with output capture:
1. Navigate to the fixed project directory
2. Run: `python -m pytest tests/ -q`
3. Capture complete test output
4. Verify all tests pass (expected: 8 passed in total)

### Task 2.3: Run Automated Tests (unittest)
Execute unittest discovery for cross-verification:
1. Run: `python -m unittest discover tests -v`
2. Capture complete test output
3. Verify all tests pass
4. Confirm consistency with pytest results

### Task 2.4: Runtime Smoke Test
Execute a runtime validation test to verify the system launches and behaves correctly:
1. Create a UserDatabase instance
2. Trigger LR rotation by inserting keys in sequence: 10, 5, 7
3. Verify tree remains balanced
4. Verify inorder traversal returns sorted sequence
5. Verify search/lookup operations work correctly

Use a Python one-liner command like:
```python
from src.user_database import UserDatabase; db=UserDatabase(); db.add_user(10,{'name':'User10'}); db.add_user(5,{'name':'User5'}); db.add_user(7,{'name':'User7'}); print('balanced:', db.is_index_balanced()); print('inorder:', db.get_all_user_ids()); print('lookup 7:', db.get_user(7))
```

Expected output:
```
balanced: True
inorder: [5, 7, 10]
lookup 7: {'name': 'User7'}
```

### Task 2.5: System Launch Verification
Verify that:
- All modules import successfully without errors
- Database instantiates correctly
- No runtime exceptions occur during basic operations
- Public API (UserDatabase) functions as expected

### Task 2.6: Consistency Verification
Ensure consistent behavior:
- Test results are consistent across pytest and unittest frameworks
- Runtime smoke test confirms expected behavior
- All validation steps complete without errors

## PART 3: COMPREHENSIVE REPORTING

### Task 3.1: Capture All Outputs
Document the following:
- Complete pytest output (format: "X passed in Y.YYs")
- Complete unittest output (format: "Ran X tests in Y.YYYs OK")
- Runtime smoke test output
- Any warnings or errors (even if non-critical)

### Task 3.2: Create Validation Summary
Provide a comprehensive summary including:

**Quick Conclusion:**
- Whether all tests passed
- Whether runtime validation succeeded
- Overall validation status

**Commands Executed:**
1. pytest command and purpose
2. unittest command and purpose  
3. Runtime smoke test command and purpose

**Full Outputs Captured:**
- pytest results
- unittest results
- Runtime smoke test results
- Note: "No error traces or exceptions were observed"

**Validation Checklist:**
- [✅/❌] Project imports and public API load without runtime errors
- [✅/❌] LR rotation behavior validated via unit tests and smoke run
- [✅/❌] All automated tests (unit + integration) pass: X passed, 0 failed
- [✅/❌] Tree remains balanced after LR-triggering operations
- [✅/❌] Search/inorder traversal return correct results
- [✅/❌] Original project left untouched; fix applied in issue_project_fixed

**Artifacts & Locations:**
- Fixed project root path
- Key files: avl_tree.py, FIX_SUMMARY.md, README.md, tests

### Task 3.3: Final Status Report
Provide a clear final statement:

```
All automated tests passed and the system launches correctly.
Result: **Validation successful — all test cases passed** and runtime smoke-check behaved as expected.
```

## SUCCESS CRITERIA

Your work is complete when ALL of the following are true:

1. ✅ New directory issue_project_fixed is created with complete project structure
2. ✅ The LR rotation bug is fixed with correct operation order (child first, parent second)
3. ✅ ALL unit tests pass (typically 8 tests total)
4. ✅ ALL integration tests pass
5. ✅ Python environment configured successfully
6. ✅ pytest execution completes with all tests passing
7. ✅ unittest execution completes with all tests passing
8. ✅ Runtime smoke test executes successfully
9. ✅ System launches without errors
10. ✅ Tree remains balanced after LR rotations
11. ✅ Search operations work correctly after rotations
12. ✅ FIX_SUMMARY.md clearly documents the fix with code comparison
13. ✅ All test outputs captured and documented
14. ✅ Final validation summary explicitly states: "all test cases passed"

## REQUIREMENTS AND CONSTRAINTS

### Must Do:
✅ Create the fixed project in NEW directory (do not modify original)
✅ Read ALL source files including __init__.py files
✅ Fix ONLY the LR rotation bug (child rotate first, parent rotate second)
✅ Configure Python environment before running tests
✅ Execute comprehensive validation (pytest + unittest + runtime smoke test)
✅ Capture all test outputs, logs, and results
✅ Create complete documentation (FIX_SUMMARY.md)
✅ Update README.md to indicate fixed version
✅ Run tests with multiple frameworks for cross-verification
✅ Execute runtime smoke test to verify system behavior
✅ Explicitly state whether all test cases passed
✅ Document any warnings or issues (even non-critical ones)

### Must NOT Do:
❌ Do NOT modify the original project directory
❌ Do NOT change the test files
❌ Do NOT modify LL, RR, or RL rotation implementations
❌ Do NOT add new dependencies to requirements.txt
❌ Do NOT skip validation steps
❌ Do NOT use multi-line Python commands in terminal (use semicolon-separated one-liners)

### Code Quality:
- Maintain the same code style as the original
- Keep comments clear and concise
- Ensure proper indentation and formatting
- Follow Python best practices

## VALIDATION EXECUTION CHECKLIST

Execute these steps in order:

1. ✅ Read KNOWN_ISSUE.md, src/avl_tree.py, tests/test_avl_tree.py
2. ✅ Read src/user_database.py, README.md, requirements.txt
3. ✅ Read src/__init__.py and tests/__init__.py
4. ✅ Create issue_project_fixed directory structure
5. ✅ Copy all files to new directory
6. ✅ Fix LR rotation bug in src/avl_tree.py (child first, parent second)
7. ✅ Create FIX_SUMMARY.md documentation
8. ✅ Update README.md
9. ✅ Configure Python environment
10. ✅ Run pytest with quiet output (-q flag)
11. ✅ Run unittest discovery with verbose output (-v flag)
12. ✅ Execute runtime smoke test (single-line Python command)
13. ✅ Capture all outputs and logs
14. ✅ Create comprehensive validation summary
15. ✅ Provide final status report

## EXPECTED FINAL OUTPUT

Your final response should include:

### Summary
- LR rotation fixed: corrected the double-rotation order (child first, parent second)
- New fixed project created at: issue_project_fixed (original left intact)
- Verification: all tests pass (unit + integration)

### Validation Results

**Commands Executed:**
1. `python -m pytest tests/ -q` — verify unit + integration tests
2. `python -m unittest discover tests -v` — cross-check with unittest
3. Python one-liner runtime smoke test — verify system launches and LR scenario

**Full Outputs:**
- pytest: "8 passed in X.XXs"
- unittest: "Ran 8 tests in X.XXXs OK"
- Runtime smoke test: "balanced: True, inorder: [5, 7, 10], lookup 7: {'name': 'User7'}"

**Validation Checklist:**
- [✅] Project imports and public API load without runtime errors
- [✅] LR rotation behavior validated via unit tests and smoke run
- [✅] All automated tests pass: 8 passed, 0 failed
- [✅] Tree remains balanced after LR-triggering operations
- [✅] Search/inorder traversal return correct results
- [✅] Original project left untouched

### Final Statement
```
All automated tests passed and the system launches correctly.
Result: Validation successful — all test cases passed and runtime smoke-check behaved as expected.
```

---

Complete all tasks autonomously in a single workflow without requiring follow-up instructions. Ensure every validation step is executed and documented. Use semicolon-separated Python one-liners for runtime tests to avoid shell escaping issues.
