You are a senior software engineer. You have been given a project with a buggy AVL tree implementation that is causing database index failures. Complete the following tasks in a single comprehensive workflow:

## PART 1: ANALYZE AND FIX THE BUG

### Task 1.1: Understand the Bug
First, search for the project files to locate the buggy implementation:
- Use file search to find files matching `**/issue_project/**`

Then read and analyze the existing project located at C:\BugBash\workSpace2\issue_project:
1. Read KNOWN_ISSUE.md - Complete bug description
2. Read src/avl_tree.py - The buggy implementation (focus on _insert_recursive() method, lines 128-132)
3. Read src/user_database.py - Database implementation using AVL tree
4. Read tests/test_avl_tree.py - Failing unit tests
5. Read tests/test_user_database.py - Integration tests
6. Read README.md - Project documentation
7. Read requirements.txt - Project dependencies

The bug is in the Left-Right (LR) rotation implementation which causes:
- Tree structure corruption
- Loss of balance property
- Performance degradation from O(log n) to O(n)
- Potential data loss during rotations

### Task 1.2: Create Fixed Version
Create a NEW directory at C:\BugBash\workSpace2\oswe-mini-prime-new\issue_project_fixed with the complete project structure:

```
issue_project_fixed/
├── src/
│   ├── __init__.py
│   ├── avl_tree.py          # FIXED VERSION - correct LR rotation order
│   └── user_database.py     # Copy from original (no changes needed)
├── tests/
│   ├── __init__.py
│   ├── test_avl_tree.py     # Copy from original
│   └── test_user_database.py # Copy from original
├── README.md                # Updated to indicate this is the fixed version
├── requirements.txt         # Same as original
└── FIX_SUMMARY.md          # NEW FILE: Document your fix
```

**Important**: Create directories first (src/ and tests/), then create files.

### Task 1.3: Implement the Fix
In src/avl_tree.py:
1. Locate the buggy LR rotation code (around lines 128-132)
2. Fix the rotation order in the LR case - implement the correct sequence
3. Add clear comments explaining the correct implementation
4. Do NOT modify LL, RR, or RL rotation implementations (they are correct)

The buggy code section:
```python
# Left-Right (LR) case - **BUG IS HERE**
if balance > 1 and key > node.left.key:
    # Current wrong implementation
    node = self.right_rotate(node)
    node.left = self.left_rotate(node.left)
    return node
```

The correct fix:
```python
# Left-Right (LR) case - FIXED
if balance > 1 and key > node.left.key:
    node.left = self.left_rotate(node.left)  # FIRST: rotate child
    return self.right_rotate(node)           # SECOND: rotate parent
```

### Task 1.4: Create Documentation
Create FIX_SUMMARY.md with:
- Problem Identified: What was wrong with the original implementation (LR case used rotations in wrong order)
- Root Cause: Why the wrong rotation order caused the bug (performed parent right-rotation before child left-rotation)
- Solution Implemented: Correct double rotation sequence (left-rotate child first, then right-rotate parent)
- Code Changes: Side-by-side comparison (before/after)
- Technical Explanation: The correct LR rotation algorithm
- Verification: Test results showing all tests pass

Update README.md to indicate this is the FIXED version and remove the "Known Issues" section.

### Task 1.5: Run Initial Tests
After creating the fixed project, run a quick test to verify the fix:
- Navigate to the fixed project directory
- Run: `python -m pytest tests/ -q` (quiet mode for quick verification)
- Verify all 9 tests pass

## PART 2: COMPREHENSIVE VALIDATION

After initial verification, execute a complete validation workflow:

### Task 2.1: Run Automated Tests (pytest - verbose)
Execute pytest test suite with verbose output:
1. Navigate to the fixed project directory
2. Run: `python -m pytest tests/ -v`
3. Capture complete test output
4. Verify all 9 tests pass
5. Expected output format:
   ```
   collected 9 items
   tests/test_avl_tree.py::TestAVLTreeLRRotation::test_lr_rotation_basic PASSED
   tests/test_avl_tree.py::TestAVLTreeLRRotation::test_lr_rotation_with_search PASSED
   tests/test_avl_tree.py::TestAVLTreeLRRotation::test_lr_rotation_complex_sequence PASSED
   tests/test_avl_tree.py::TestAVLTreeLRRotation::test_lr_rotation_performance PASSED
   tests/test_avl_tree.py::TestAVLTreeOtherRotations::test_ll_rotation PASSED
   tests/test_avl_tree.py::TestAVLTreeOtherRotations::test_rr_rotation PASSED
   tests/test_avl_tree.py::TestAVLTreeOtherRotations::test_rl_rotation PASSED
   tests/test_user_database.py::TestUserDatabaseIntegration::test_user_database_lr_scenario PASSED
   tests/test_user_database.py::TestUserDatabaseIntegration::test_bulk_user_insertion PASSED
   ====== 9 passed in X.XXs ======
   ```

### Task 2.2: Run Automated Tests (unittest - verbose)
Execute unittest discovery for cross-verification:
1. Run: `python -m unittest discover tests -v`
2. Capture complete test output
3. Verify all 9 tests pass
4. Expected output format: "Ran 9 tests in X.XXXs OK"
5. Confirm consistency with pytest results

### Task 2.3: Create and Execute Smoke Test Script
Create a runtime smoke test script to verify system behavior:

**Create smoke_run.py:**
```python
from src.user_database import UserDatabase

print("Starting smoke test for UserDatabase...")

# Create database and add users (triggers LR rotation with 1000, 500, 750)
db = UserDatabase()
db.add_user(1000, {'name': 'Alice Smith'})
db.add_user(500, {'name': 'Jane Doe'})
db.add_user(750, {'name': 'Bob Johnson'})

# Verify functionality
all_ids = db.get_all_user_ids()
print(f"All user IDs (inorder): {all_ids}")

is_balanced = db.is_index_balanced()
print(f"Is index balanced?: {is_balanced}")

user_750 = db.get_user(750)
print(f"Get user 750: {user_750}")

print("Smoke test completed successfully.")
```

**Execute smoke test:**
- Run: `python smoke_run.py`
- Expected output:
  ```
  Starting smoke test for UserDatabase...
  All user IDs (inorder): [500, 750, 1000]
  Is index balanced?: True
  Get user 750: {'name': 'Bob Johnson'}
  Smoke test completed successfully.
  ```

This verifies:
- Database instantiation works
- Insert operations trigger LR rotation (1000, 500, 750 sequence)
- Tree remains balanced after LR rotation
- Inorder traversal returns sorted sequence [500, 750, 1000]
- Search/lookup operations work correctly

### Task 2.4: Capture Runtime Environment Info
Capture environment details:
- Run: `python -c "import pytest, sys; print('pytest', pytest.__version__); import platform; print(platform.platform())"`
- Expected output format: "pytest X.X.X" and platform information

### Task 2.5: Read FIX_SUMMARY.md
Read the FIX_SUMMARY.md file to verify documentation is complete.

### Task 2.6: System Launch Verification
Verify that:
- All modules import successfully without errors
- UserDatabase instantiates correctly
- No runtime exceptions occur during operations
- Public API functions as expected

## PART 3: COMPREHENSIVE REPORTING

### Task 3.1: Capture All Outputs
Document the following:

**Locations:**
- Fixed project path: C:\BugBash\workSpace2\oswe-mini-prime-new\issue_project_fixed
- Key files:
  - avl_tree.py (LR rotation fixed)
  - FIX_SUMMARY.md (documenting fix)
  - smoke_run.py (runtime smoke script)

**Actions Performed:**
1. Full pytest suite (verbose) - `python -m pytest tests/ -v`
2. Unittest discovery (verbose) - `python -m unittest discover tests -v`
3. Smoke runtime script exercising the main API - `python smoke_run.py`
4. Captured runtime environment info (pytest version, platform)

**Outputs Captured:**
- Pytest (verbose):
  - "collected 9 items"
  - All tests passed: 9 passed in X.XXs
  - See excerpt: tests/test_avl_tree.py::... PASSED, tests/test_user_database.py::... PASSED

- Unittest discovery:
  - Ran 9 tests — OK

- Smoke script output:
  - Starting smoke test for UserDatabase...
  - All user IDs (inorder): [500, 750, 1000]
  - Is index balanced?: True
  - Get user 750: {'name': 'Bob Johnson'}
  - Smoke test completed successfully.

- Runtime environment:
  - pytest version
  - Platform information

### Task 3.2: Create Validation Summary
Provide a comprehensive summary:

**Summary of Actions & Results:**

**What I ran:**
1. Full pytest suite (verbose) - Command: `python -m pytest tests/ -v`
2. Unittest discovery (verbose) - Command: `python -m unittest discover tests -v`
3. Smoke runtime script exercising the main API - Script: smoke_run.py, Command: `python smoke_run.py`
4. Captured runtime environment info (pytest version, platform)

**Outputs (captured):**
- Pytest (verbose): "collected 9 items", All tests passed: 9 passed in X.XXs
- Unittest discovery: Ran 9 tests — OK
- Smoke script output: IDs, balance status, user lookup successful
- Runtime environment: pytest version and platform

**Logs & Files of Interest:**
- Fixed project path with key files listed

### Task 3.3: Final Status Report
Provide a clear final statement:

```
All automated test cases passed (9/9).
The system launches and the UserDatabase API behaves correctly in the smoke run.
No errors or failures were observed.
```

## SUCCESS CRITERIA

Your work is complete when ALL of the following are true:

1. ✅ File search performed to locate issue_project files
2. ✅ New directory issue_project_fixed is created with complete project structure
3. ✅ Directories created first (src/ and tests/), then files
4. ✅ The LR rotation bug is fixed with correct operation order (child first, parent second)
5. ✅ ALL 9 unit tests pass (0 failures)
6. ✅ ALL integration tests pass
7. ✅ Initial pytest test run completed (pytest -q)
8. ✅ pytest execution completes with all 9 tests passing (verbose mode)
9. ✅ unittest execution completes with all 9 tests passing (verbose mode)
10. ✅ smoke_run.py script created and executed successfully
11. ✅ Runtime environment info captured (pytest version, platform)
12. ✅ FIX_SUMMARY.md read and verified
13. ✅ System launches without errors
14. ✅ Tree remains balanced after LR rotations
15. ✅ Search operations work correctly after rotations
16. ✅ Inorder traversal returns sorted sequence [500, 750, 1000]
17. ✅ FIX_SUMMARY.md clearly documents the fix with code comparison
18. ✅ README.md updated to indicate fixed version
19. ✅ All test outputs captured and documented
20. ✅ Final validation summary explicitly states: "All automated test cases passed (9/9). The system launches and the UserDatabase API behaves correctly in the smoke run. No errors or failures were observed."

## REQUIREMENTS AND CONSTRAINTS

### Must Do:
✅ Search for issue_project files first
✅ Create the fixed project in NEW directory (do not modify original)
✅ Create directories BEFORE creating files
✅ Read ALL source files (KNOWN_ISSUE.md, avl_tree.py, user_database.py, test files, README, requirements)
✅ Fix ONLY the LR rotation bug (child rotate first, parent rotate second)
✅ Run initial quick test (pytest -q) after creating fix
✅ Execute comprehensive validation (pytest -v + unittest -v + smoke script)
✅ Create smoke_run.py script with specific test scenario (1000, 500, 750)
✅ Capture runtime environment info
✅ Read FIX_SUMMARY.md to verify documentation
✅ Capture all test outputs, logs, and results
✅ Create complete documentation (FIX_SUMMARY.md)
✅ Update README.md to indicate fixed version
✅ Run tests with both frameworks for cross-verification
✅ Explicitly state whether all test cases passed
✅ Document that no errors or failures were observed
✅ Verify 9 tests pass in total

### Must NOT Do:
❌ Do NOT modify the original project directory
❌ Do NOT change the test files
❌ Do NOT modify LL, RR, or RL rotation implementations
❌ Do NOT add new dependencies to requirements.txt
❌ Do NOT skip validation steps

### Code Quality:
- Maintain the same code style as the original
- Keep comments clear and concise
- Ensure proper indentation and formatting
- Follow Python best practices
- Add explanatory comments for the fixed LR rotation code

## VALIDATION EXECUTION CHECKLIST

Execute these steps in order:

1. ✅ Search for issue_project files (`**/issue_project/**`)
2. ✅ Read KNOWN_ISSUE.md, src/avl_tree.py
3. ✅ Read src/user_database.py
4. ✅ Read tests/test_avl_tree.py, tests/test_user_database.py
5. ✅ Read README.md, requirements.txt
6. ✅ Create issue_project_fixed directory
7. ✅ Create src/ and tests/ subdirectories
8. ✅ Create all source files (fix avl_tree.py, copy others)
9. ✅ Create test files (copy unchanged)
10. ✅ Read and create README.md (updated)
11. ✅ Read and create requirements.txt
12. ✅ Create FIX_SUMMARY.md documentation
13. ✅ Run initial test (python -m pytest tests/ -q)
14. ✅ Run pytest with verbose output (-v flag)
15. ✅ Run unittest discovery with verbose output (-v flag)
16. ✅ Create smoke_run.py script
17. ✅ Execute smoke test (python smoke_run.py)
18. ✅ Capture runtime environment info
19. ✅ Read FIX_SUMMARY.md
20. ✅ Create comprehensive validation summary
21. ✅ Provide final status report

## EXPECTED FINAL OUTPUT

Your final response should include:

### Fix Complete Summary
- LR rotation bug resolved & tests passing ✅
- Created fixed copy of project under issue_project_fixed
- Fixed LR rotation order in avl_tree.py (left-rotate child first, then right-rotate parent)
- All tests pass: 9 passed, 0 failures (unit + integration)

### What Changed
- File: avl_tree.py
- Issue: LR case used rotations in wrong order (performed parent right-rotation before child left-rotation)
- Fix: Implemented correct double rotation sequence with explanatory comments
- Files added/copied with complete structure listed

### Why This Fixes the Bug
- LR is a double-rotation. Correct sequence:
  1. left-rotate the left child (straighten zig-zag)
  2. right-rotate the parent (rebalance)
- Reversing the sequence corrupts links and can make nodes unreachable
- The fix restores the AVL invariant and search correctness

### Summary of Actions & Results

**What I ran:**
1. Full pytest suite (verbose) - Command: `python -m pytest tests/ -v`
2. Unittest discovery (verbose) - Command: `python -m unittest discover tests -v`
3. Smoke runtime script exercising the main API - Script: smoke_run.py (created and executed), Command: `python smoke_run.py`
4. Captured runtime environment info (pytest version, platform)

**Outputs (captured):**
- Pytest (verbose): "collected 9 items", All tests passed: 9 passed in X.XXs, See excerpt: tests/test_avl_tree.py::... PASSED, tests/test_user_database.py::... PASSED
- Unittest discovery: Ran 9 tests — OK
- Smoke script output:
  - Starting smoke test for UserDatabase...
  - All user IDs (inorder): [500, 750, 1000]
  - Is index balanced?: True
  - Get user 750: {'name': 'Bob Johnson'}
  - Smoke test completed successfully.
- Runtime environment: pytest version and platform

**Logs & Files of Interest:**
- Fixed project path: C:\BugBash\workSpace2\oswe-mini-prime-new\issue_project_fixed
- Key files: avl_tree.py (LR rotation fixed), FIX_SUMMARY.md (documenting fix), smoke_run.py (runtime smoke script)

### Final Verification Statement
**All automated test cases passed (9/9). The system launches and the UserDatabase API behaves correctly in the smoke run. No errors or failures were observed.**

---

Complete all tasks autonomously in a single workflow without requiring follow-up instructions. Ensure every validation step is executed and documented. Create smoke_run.py script for runtime validation and capture all outputs.
