You are a senior software engineer. You have been given a project with a buggy AVL tree implementation that is causing database index failures. Complete the following tasks in a single comprehensive workflow:

## PART 1: ANALYZE AND FIX THE BUG

### Task 1.1: Understand the Bug
Read and analyze the existing project located at C:\BugBash\workSpace3\issue_project:
1. Read KNOWN_ISSUE.md - Complete bug description
2. Read src/avl_tree.py - The buggy implementation (focus on _insert_recursive() method, lines 128-132)
3. Read tests/test_avl_tree.py - Failing unit tests
4. Read src/user_database.py - Database implementation using AVL tree
5. Read requirements.txt - Project dependencies

The bug is in the Left-Right (LR) rotation implementation which causes:
- Tree structure corruption
- Loss of balance property
- Performance degradation from O(log n) to O(n)
- Potential data loss during rotations

### Task 1.2: Create Fixed Version
Create a NEW directory at C:\BugBash\workSpace3\Claude-haiku-4.5\issue_project_fixed with the complete project structure:

```
issue_project_fixed/
├── src/
│   ├── __init__.py
│   ├── avl_tree.py          # FIXED VERSION - correct LR rotation order
│   └── user_database.py     # Copy from original (no changes needed)
├── tests/
│   ├── __init__.py
│   ├── test_avl_tree.py     # Copy from original
│   └── test_user_database.py # Copy from original
├── README.md                # Updated to indicate this is the fixed version
├── requirements.txt         # Same as original
└── FIX_SUMMARY.md          # NEW FILE: Document your fix
```

### Task 1.3: Implement the Fix
In src/avl_tree.py:
1. Locate the buggy LR rotation code (around lines 128-132)
2. Fix the rotation order in the LR case - determine and implement the correct sequence
3. Add clear comments explaining the correct implementation
4. Do NOT modify LL, RR, or RL rotation implementations (they are correct)

The buggy code section:
```python
# Left-Right (LR) case - **BUG IS HERE**
if balance > 1 and key > node.left.key:
    # Current wrong implementation
    node = self.right_rotate(node)
    node.left = self.left_rotate(node.left)
    return node
```

### Task 1.4: Create Documentation
Create FIX_SUMMARY.md with:
- Problem Identified: What was wrong with the original implementation
- Root Cause: Why the wrong rotation order caused the bug
- Solution Implemented: What changed and why
- Code Changes: Side-by-side comparison (before/after)
- Technical Explanation: The correct LR rotation algorithm
- Verification: How you verified the fix works

Update README.md to indicate this is the FIXED version and remove the "Known Issues" section.

## PART 2: COMPREHENSIVE VALIDATION

After creating the fixed project, execute a complete validation workflow:

### Task 2.1: Environment Setup
Configure the Python environment for the fixed project at C:\BugBash\workSpace3\Claude-haiku-4.5\issue_project_fixed.

### Task 2.2: Run Automated Tests
Execute all automated test suites:
1. Run pytest with verbose output: `python -m pytest tests/ -v --tb=short`
2. Run unittest discovery as alternative validation: `python -m unittest discover -s tests -p "test_*.py" -v`
3. Capture all test outputs and results

Expected test results (all must pass):
- test_lr_rotation_basic
- test_lr_rotation_with_search
- test_lr_rotation_complex_sequence
- test_lr_rotation_performance
- test_ll_rotation
- test_rr_rotation
- test_rl_rotation
- test_user_database_lr_scenario
- test_bulk_user_insertion

### Task 2.3: Manual Validation Tests
Execute comprehensive manual validation tests to verify:
1. Basic LR Rotation (10, 5, 7 pattern)
2. Search operations after LR rotation
3. Complex insertion sequences with multiple rotations
4. All nodes remain reachable after rotations
5. User database integration with LR rotation pattern
6. LL rotation verification (comparison)
7. RR rotation verification (comparison)
8. RL rotation verification (comparison)
9. Large dataset performance test (100 nodes, verify O(log n))
10. Edge cases and boundary conditions

### Task 2.4: System Launch Verification
Verify that:
- All modules import successfully without errors
- No runtime exceptions occur
- System is ready for operation

### Task 2.5: Create Validation Documentation
Create VALIDATION_REPORT.md containing:
- Executive summary with key metrics
- Detailed test execution results (all test cases)
- Performance analysis and scalability verification
- Success criteria validation
- Final validation status

## PART 3: FINAL DELIVERABLES

### Task 3.1: Create Execution Summary
Create EXECUTION_SUMMARY.txt at C:\BugBash\workSpace3\Claude-haiku-4.5\ with:
- Project overview and status
- Complete test results summary
- Validation tasks completed checklist
- Final recommendation (production readiness)

### Task 3.2: Verify Completeness
Confirm that all deliverables are created:
- ✓ Fixed project directory with all source files
- ✓ FIX_SUMMARY.md documenting the bug fix
- ✓ Updated README.md
- ✓ VALIDATION_REPORT.md with comprehensive test results
- ✓ EXECUTION_SUMMARY.txt with final summary
- ✓ All tests passing (0 failures, 100% success rate)

## SUCCESS CRITERIA

Your work is complete when ALL of the following are true:

1. ✅ New directory issue_project_fixed is created with complete project structure
2. ✅ The LR rotation bug is fixed with correct operation order
3. ✅ ALL 9 unit tests pass (0 failures)
4. ✅ ALL integration tests pass (0 failures)
5. ✅ Tree remains balanced after LR rotations
6. ✅ Search operations work correctly after rotations
7. ✅ Tree height remains logarithmic (O(log n))
8. ✅ FIX_SUMMARY.md clearly documents the fix with code comparison
9. ✅ VALIDATION_REPORT.md shows comprehensive test results
10. ✅ Manual validation confirms all 10 test scenarios pass
11. ✅ System launches without errors
12. ✅ EXECUTION_SUMMARY.txt provides final status report
13. ✅ Final validation status explicitly states: "ALL TESTS PASSED"

## REQUIREMENTS AND CONSTRAINTS

### Must Do:
✅ Create the fixed project in NEW directory (do not modify original)
✅ Fix ONLY the LR rotation bug
✅ Ensure ALL tests pass after the fix
✅ Execute comprehensive validation (automated + manual tests)
✅ Create complete documentation (FIX_SUMMARY.md, VALIDATION_REPORT.md, EXECUTION_SUMMARY.txt)
✅ Capture all test outputs, logs, and results
✅ Explicitly state whether all test cases passed
✅ Confirm production readiness

### Must NOT Do:
❌ Do NOT modify the original project directory
❌ Do NOT change the test files
❌ Do NOT modify LL, RR, or RL rotation implementations
❌ Do NOT add new dependencies or change project structure significantly

### Code Quality:
- Maintain the same code style as the original
- Keep comments clear and concise
- Ensure proper indentation and formatting
- Follow Python best practices

## FINAL OUTPUT REQUIREMENT

At the end, provide a concise summary confirming:
1. The bug fix is implemented correctly
2. All automated tests passed (specify count: X/X passed)
3. All manual validation tests passed (specify count: X/X passed)
4. Total test cases executed and success rate
5. Final status: "ALL TESTS PASSED - READY FOR PRODUCTION DEPLOYMENT"

Complete all tasks autonomously in a single workflow without requiring follow-up instructions.
